# Daily Notes — 2026-03-30

## Session summary (late night 2026-03-29 into 2026-03-30)

---

## Navigation refactor (bottom tabs)

Replaced hamburger menu with bottom tab bar (`@react-navigation/bottom-tabs`).

**Tab structure:**
- Student: Home / Programs / Messages / Profile
- Coach: Home / Students / Programs / Profile
- Admin: Dashboard / Students / Programs / Messages
- Super Admin: Dashboard / Users / Programs / Profile

**Key files changed:**
- `src/navigation/index.tsx` — full rewrite with nested Tab+Stack navigators
- `src/components/common/ScreenHeader.tsx` — hamburger removed, logo uses `canGoBack() ? popToTop() : null`
- All home screens — responsive card widths via `Dimensions`, safe paddingBottom for tab bar

**Cross-tab navigation fixes:**
- `CoachHomeScreen` grid uses `navigation.getParent()?.navigate(tab, { screen })` for Programs→ProgramsTab, Students→StudentsTab
- Student HomeScreen: Programs/Messages use `getParent()?.navigate(tab)`
- Student MessagesScreen: Messages→`MessagesTab`, not inline navigate

---

## Super Admin changes

- Lands on Dashboard tab by default (not Users)
- Users tab is second tab (user list + role management)
- `SuperAdminDashboardStack` now includes: Students, StudentDetail, Attendance, CoachNotes, PromotionManage, ManageVideos, UploadVideo, Payments, BulkMsg, Announce, AcademySettings, BillingSettings, DivisionDashboard, TournamentManage, Chat, NewConversation
- Stats bar in Users tab: horizontal ScrollView, full labels, active underline indicator
- Platform Management grid removed from Users tab (lives on Dashboard)

---

## Fabian's role fixed

- Changed from student (conflicting with super_admin) to `super_admin` only
- Done via direct DB PATCH (service role key)
- Coach notes student list now filters to `role = 'student'` only

---

## Coach Notes RLS fix

- Added `super_admin` to INSERT/UPDATE/DELETE policies on `coach_notes` table
- Applied via psql directly (supabase CLI push was failing with `--project-ref` flag incompatibility)
- Migration file written: `supabase/migrations/20260330000001_fix_coach_notes_super_admin.sql`

---

## Coach Notes → Messages tab (student side)

- Merged coach notes into student Messages tab as a single **folder card** (not expanded list)
- Card shows: note count badge, latest note preview, taps to open `MyCoachNotesScreen`
- `MyCoachNotesScreen` added to `StudentMessagesStack` in navigation
- Tab title: "Messages" (reverted from "Messages & Notes")
- Coach Notes removed from Home quick actions grid

---

## Email from address

- Changed from `office@bptacademy.uk` → `hello@bptacademy.uk`
- Updated in all 3 edge functions: send-notification, process-notifications, weekly-summary
- All redeployed to Supabase

---

## pm2 dev server

- Switched back to `--tunnel` mode (from LAN) for remote testers
- Current tunnel URL: `exp://af-r3um-bptacademyuk-8081.exp.direct` (may change on restart)
- Testers: open Safari on iPhone → paste tunnel URL → Expo Go launches
- Testers must log into Expo Go as `bptacademyuk`

---

## Team testers

- **Andrei** — iOS tester, using Expo Go with `bptacademyuk` account
- Android testers can use APK: https://expo.dev/accounts/bptacademyuk/projects/mobile/builds/15a0c2c2-049b-47d5-b788-e79e85cb4dc7
- iOS standalone build pending Apple Developer account from Fabian

---

## Navigation errors fixed (this session)

1. `Manage`/`Videos`/`Students` not found — cross-tab navigation fixed in CoachHomeScreen
2. `popToTop` error on logo tap on root screens — fixed with `canGoBack()` guard
3. `MyCoachNotes` not found from Messages tab — added to StudentMessagesStack
4. `CoachNotes`/`PromotionManage` not found from StudentDetail in SuperAdmin — added to SuperAdminDashboardStack

---

## Pending / next steps

- ⚠️ Apple Developer account — Fabian creating, needed for iOS EAS build + TestFlight
- ⚠️ pg_cron schedules not yet set up in Supabase for notification edge functions
- ⚠️ Git commit — all navigation changes not yet committed
- Test all nav flows on real devices across all roles
- Android EAS build may need rebuild with latest nav changes
