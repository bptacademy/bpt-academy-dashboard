# 2026-03-30

## Morning/Afternoon Session

### Navigation overhaul ✅
- Replaced hamburger menu with bottom tab bar (4 tabs per role)
- Super Admin tabs: Dashboard / Users / Messages / Profile (Programs tab replaced with Messages)
- Logo tap uses `popToTop()` safely — fixed crash via `navigation.getState().index > 0`
- Back button (`BackHeader`) also uses same safe check

### Super Admin fixes ✅
- `goToPrograms` now navigates within Dashboard stack (not ProgramsTab) for super_admin
- `SuperAdminDashboardStack` now includes: Manage, ProgramRoster, ProgramModules, VideoPlayer
- `SuperAdminMessagesScreen` created — proper messages hub with Bulk Message + Announcements + DMs + Groups

### Video player ✅
- Works for all roles (student/coach/admin/super_admin)
- Bookmarks only shown for students
- Comment author names now display correctly

### Division Dashboard fix ✅
- Was showing academy-wide totals for every division (bug: missing division filter)
- Now correctly fetches per-division: students by profile.division, enrollments via program.division, revenue via payment→enrollment→program.division

### ngrok / tunnel setup ✅
- Installed ngrok v3 via brew
- Auth token saved: `~/.ngrok2/ngrok.yml` (v2 format, used by @expo/ngrok-bin)
- ngrok API key needed for static domain reservation (auth token ≠ API key)
- Current tunnel URL: `exp://9ol67bm-bptacademyuk-8081.exp.direct` (changes on restart)
- pm2 config: restart_delay 8000ms, no max_restarts cap
- Tunnel still occasionally drops — ngrok free .exp.direct format used by expo/ngrok-bin v2.3.41

### Dev server
- pm2 process: `bpt-academy`
- Mode: `--tunnel`
- Config: `bpt-academy/mobile/ecosystem.config.js`
- Helper script: `bpt-academy/mobile/get-url.sh` — prints current tunnel URL

### ngrok credentials
- Auth token: 3BftKaqkXBCmqqH6zH5fr7ziyeL_2sY31pCDdWD97jSQokCEu
- API key: needed from dashboard.ngrok.com/api-keys (not yet saved)
- Paid account (Pay-as-you-go, $20/month)

### LAN fallback
- Mac IP: 10.0.24.97
- LAN URL: `exp://10.0.24.97:8081` (requires same WiFi)
- To switch to LAN: change ecosystem.config.js args to `expo start --lan`
