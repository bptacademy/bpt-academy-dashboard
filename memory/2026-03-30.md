# 2026-03-30

## BPT Academy — Bottom Tab Navigation (during the day)

- Replaced hamburger menu + MenuDrawer with bottom tab bar (`@react-navigation/bottom-tabs`)
- 4 tabs per role (Student / Coach / Admin / Super Admin)
- Tab bar auto-handles iPhone safe area (no fixed height)
- Logo uses `popToTop()` — works for all roles
- Quick action grid cross-tab navigation fixed (Messages uses `getParent()?.navigate`)
- Responsive card widths using `Dimensions.get('window')` — works on all screen sizes
- Super Admin lands on Dashboard tab by default (Users tab is second)
- SuperAdminHomeScreen Users tab: stats bar now horizontal scroll, full labels

## BPT Academy — Fabian's Role Fixed

- Changed from student (conflicting) to `super_admin` only in DB
- Done via direct PATCH to profiles table (service role key)

## BPT Academy — coach_notes RLS Fix

- New migration: `20260330000001_fix_coach_notes_super_admin.sql`
- Fixed coach_notes RLS to allow super_admin write access
- Policies updated: insert / update / delete now include `super_admin` in role check
- **Note:** This migration is local-only — not yet applied to remote Supabase

## Nightly Backup (8:00 PM)

- Ran `/bpt-academy/backups/nightly-backup.sh` — completed ✅ (at 20:00:15)
- Git: workspace + bpt-academy both auto-committed
- Migrations snapshot saved: `migrations_20260330_200015.sql`
- Migration sync check:
  - All migrations up to `20260327000001` synced with remote ✅
  - Migrations `20260328000001–20260328000008` — local-only ⚠️
  - Migration `20260330000001` — local-only ⚠️
- Credentials snapshot saved: `credentials_20260330_200015.md`

---

## Evening Session — Full Day Summary

### Navigation fixes ✅
- Super Admin tabs: Dashboard / Users / Messages / Profile (Programs replaced with Messages)
- Logo tap: now uses `navigation.getState().index > 0` — no more POP_TO_TOP crash
- Logo at root tab: navigates to first tab (Home/Dashboard) via `getParent().getState().routeNames[0]`
- All stacks fully registered — no more "screen not handled" errors for any role
- Coach tabs: Home / Students / Messages / Profile (Programs accessible from Home quick actions)

### Super Admin fixes ✅
- `goToPrograms` navigates within Dashboard stack (not missing ProgramsTab)
- `SuperAdminDashboardStack` includes: Manage, ProgramRoster, ProgramModules, VideoPlayer + all others
- `SuperAdminMessagesScreen` created — hub with Bulk Message, Announcements, DMs, Groups

### Coach fixes ✅
- Upload Video / Programs now navigate within HomeStack (no cross-tab)
- Attendance quick action routes to Students tab (needs session ID — can't open standalone)
- Messages tab added to coach tab bar
- CoachDashboardScreen: Messages quick action added, stats fixed

### Profile screen ✅
- Added `navigation` prop
- Admin Tools section added (for super_admin / admin / coach):
  - 📋 Attendance (via Programs → Roster)
  - 👥 Manage Students
  - 🏅 Divisions
  - 🎬 Manage Videos
- All profile stacks updated to include admin tool screens

### Video player ✅
- Fixed for all roles — bookmarks only for students
- Comment author names display correctly
- No autoplay on open

### Division Dashboard fix ✅
- Was showing academy-wide totals for ALL divisions (bug: no division filter)
- Fixed: enrollments filtered via program.division, revenue via payment→enrollment→program.division
- Per-division revenue now shown on each division card

### ProgramModulesScreen ✅
- Added green FAB (+) button to add new modules manually
- Edit modal: title + description + student preview
- Delete: long press card OR button inside edit modal
- No longer relies only on auto-generated modules

### New screens created ✅
1. `ReportsScreen.tsx` (superadmin/) — weekly/monthly reports with Excel export
2. `CalendarDayScreen.tsx` (student/) — day detail view for calendar events
3. `SuperAdminMessagesScreen.tsx` (superadmin/) — messaging hub

### Updated screens ✅
- `HomeScreen.tsx` — interactive calendar strip (7 days back + today + 7 forward)
  - Today highlighted green, red dot on days with program sessions
  - Local date strings (no UTC off-by-one bug)
  - Sessions mapped to CalendarEvent shape before passing to CalendarDayScreen
- `TournamentListScreen.tsx` — LTA National Padel Tour 2026 section at top
  - Dark banner, next event highlighted, horizontal scroll of G1/G2 fixtures
  - "View Full LTA Calendar →" opens competitions.lta.org.uk
  - Note: LTA site blocked by cookie wall — fixtures are approximate; real data link is in app
- `CoachHomeScreen.tsx` — added 📊 Reports quick action

### Excel Export (Reports) ✅
- Libraries installed: `xlsx` + `expo-sharing`
- Generates `.xlsx` with 4 sheets: Summary, Revenue by Division, Students by Division, Top Students
- Uses `expo-file-system` to write then `expo-sharing` to open iOS/Android share sheet
- Works on both iOS and Android
- No new native build required (pure JS + Expo managed)

### ngrok / tunnel setup ✅
- ngrok v3 installed via brew
- Auth token: `3BftKaqkXBCmqqH6zH5fr7ziyeL_2sY31pCDdWD97jSQokCEu` (saved in ~/.ngrok2/ngrok.yml)
- API key: `3Bg1cR2knlTv7OoUZ2etszw5Y82_5jkgRSn14mr24cYVSRNZH`
- ngrok paid account (Pay-as-you-go, $20/month)
- `@expo/ngrok-bin-darwin-arm64` v2 binary used by Expo (v2.3.42)
- Auth token set on v2 binary via: `node_modules/@expo/ngrok-bin-darwin-arm64/ngrok authtoken <token>`
- `AsyncNgrok.js` patched: Expo's shared token replaced with paid token, domain changed to ngrok.app
- `EXPO_TUNNEL_SUBDOMAIN=bpt-academy` set in ecosystem.config.js
- Static domain reserved: `bpt-academy.ngrok.app` (via API)
- Current working tunnel URL: `exp://bpt-academy.ngrok.io` ← use this permanently
- LAN fallback: `exp://10.0.24.97:8081` (same WiFi only)
- Helper script: `bpt-academy/mobile/get-url.sh`

### Dev server ✅
- pm2 process: `bpt-academy`
- Mode: `expo start --tunnel`
- Config: `bpt-academy/mobile/ecosystem.config.js`
- restart_delay: 8000ms, no max_restarts cap
- pm2 save done

### Pending / carry forward ⚠️
- Apple Developer account — Fabian still needs to create for iOS EAS build / TestFlight
- pg_cron schedules not yet configured in Supabase for notification edge functions
- Local migrations 20260328* and 20260330* not yet applied to remote Supabase
- LTA fixture data is approximate — real data blocked by LTA cookie wall
- Josep Aymerich not yet in DB (coach account not registered)
