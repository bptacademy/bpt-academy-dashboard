# 2026-03-30

## BPT Academy — Bottom Tab Navigation (during the day)

- Replaced hamburger menu + MenuDrawer with bottom tab bar (`@react-navigation/bottom-tabs`)
- 4 tabs per role (Student / Coach / Admin / Super Admin)
- Tab bar auto-handles iPhone safe area (no fixed height)
- Logo uses `popToTop()` — works for all roles
- Quick action grid cross-tab navigation fixed (Messages uses `getParent()?.navigate`)
- Responsive card widths using `Dimensions.get('window')` — works on all screen sizes
- Super Admin lands on Dashboard tab by default (Users tab is second)
- SuperAdminHomeScreen Users tab: stats bar now horizontal scroll, full labels

## BPT Academy — Fabian's Role Fixed

- Changed from student (conflicting) to `super_admin` only in DB
- Done via direct PATCH to profiles table (service role key)

## BPT Academy — coach_notes RLS Fix

- New migration: `20260330000001_fix_coach_notes_super_admin.sql`
- Fixed coach_notes RLS to allow super_admin write access
- Policies updated: insert / update / delete now include `super_admin` in role check
- **Note:** This migration is local-only — not yet applied to remote Supabase

## Nightly Backup (8:00 PM)

- Ran `/bpt-academy/backups/nightly-backup.sh` — completed ✅ (at 20:00:15)
- Git: workspace + bpt-academy both auto-committed (`chore: nightly auto-backup 2026-03-30`)
- Migrations snapshot saved: `migrations_20260330_200015.sql`
- Migration sync check:
  - All migrations up to `20260327000001` synced with remote ✅
  - Migrations `20260328000001–20260328000008` — local-only ⚠️ (not yet applied to remote)
  - Migration `20260330000001` — local-only ⚠️ (not yet applied to remote)
- Credentials snapshot saved: `credentials_20260330_200015.md`
- Old backups cleaned (kept last 7 days)
- No new keys or credentials added today
