# 2026-03-26

## BPT Promotion Evaluator — 18:00 UTC

Cron job fired: `evaluate_all_promotion_cycles()` via Supabase RPC.
- URL: https://nobxhhnhakawhbimrate.supabase.co/rest/v1/rpc/evaluate_all_promotion_cycles
- Result: `1` (success — function returned 1, indicating at least one promotion cycle was evaluated/processed)

---

## Evening Session — 22:00–23:09 UTC

### Bugs Fixed (all server-side unless noted)

**1. Josep's enrollment stuck as `cancelled`**
- Root cause: duplicate `student_id + program_id` unique constraint blocked re-enrollment
- Fix: migration `20260326000010` fixed `sync_profile_division_on_enroll` trigger (text→division_type cast error)
- Josep's enrollment manually reactivated to `active` via Management API
- Josep profile: `division: pro`, `skill_level: null`

**2. Promotion cycle didn't start for Josep (Pro player)**
- Root cause: Pro is top level — `start_promotion_cycle_for_student` correctly returns early for Pro
- Fix (app code, needs APK): `PromotionManageScreen` now fetches student division; shows 🏆 "Pro Division — Top Level" message and hides Start button for Pro players
- Fix (app code, needs APK): `ProgressScreen` Promotion tab shows 🏆 "You're at the top!" for Pro players

**3. Infinite refresh loop on Promotion tab**
- Root cause: `profile` object is new reference every render → `useCallback` deps `[profile]` → new function every render → `useEffect` fires → infinite loop
- Fix: `AuthContext.refreshProfile()` now uses `JSON.stringify` comparison before calling `setProfile` (only updates if data actually changed); stabilised with `useCallback` on `session.user.id`
- Fix: All `useCallback` deps in `ProgressScreen` changed from `[profile]` to `[profile?.id]` (primitive, stable)
- Fix: `fetchCycle` now calls `setCycleLoading(false)` when profile is null (was leaving spinner forever)

**4. HomeScreen didn't refresh after enrollment**
- Root cause: `useEffect(() => fetchData(), [profile])` — no focus refetch
- Fix: Added `useFocusEffect` to `HomeScreen` — re-fetches on every screen focus

**5. Re-enrollment unique constraint error**
- Fix: `ProgramDetailScreen` free enroll path now checks for existing cancelled row and reactivates it instead of inserting duplicate

**6. New user registration "Database error saving new user"**
- Root cause: `handle_new_user` trigger inserting raw `text` into `division_type` and `skill_level` enum columns — no cast
- Fix: migration `20260326000012` — trigger now safely casts with `exception when others → null` fallback
- Server-side only, live immediately

**7. "Could not start conversation" when sending DM**
- Root cause: new `conversation_members` insert RLS policy blocked coaches from inserting the student's membership row (only allowed self-insert or coach role, not "creator inserting for other participant")
- Fix: migration `20260326000013` — creator of a direct conversation can insert both member rows
- Server-side only, live immediately

### Feature: Messaging Privacy + Program Group Channels (migration 20260326000011)
- Added `conversation_type: 'direct' | 'program_group'` to conversations table
- Added `program_id` FK to conversations (links group channels to programs)
- RLS tightened: DM messages visible only to the 2 exact members
- Trigger `auto_create_program_channel`: creates group channel when program is created
- Trigger `sync_program_channel_membership`: auto-adds/removes students on enrollment status change
- Backfilled group channels for all existing programs (Amateur Beginners, Pro Division, Semi-Pro)
- `MessagesScreen` (app, needs APK): split into 🔒 Direct Messages + 👥 Program Channels sections
- Students: no compose FAB (can't initiate DMs); coaches/admins have FAB
- `StudentDetailScreen` + `NewConversationScreen`: tag new DMs as `conversation_type: 'direct'`

### Migrations Applied Today (total: 28, range 20260322000000–20260326000013)
- `20260326000010`: fix division trigger cast + Pro-level start_promotion_cycle guard
- `20260326000011`: messaging privacy + program group channels
- `20260326000012`: fix handle_new_user enum cast (registration fix)
- `20260326000013`: fix DM RLS for conversation_members insert

### Commits Today
- `dcc789a` fix: infinite buffer, Pro top-level state, re-enrollment upsert, division trigger cast
- `a171cde` fix: Pro players show top-level message instead of broken cycle UI
- `5360ff4` fix: HomeScreen re-fetches on focus
- `d5e6d08` fix: infinite refresh loop on Promotion tab
- `355ebff` feat: messaging privacy — private DMs + program group channels
- `8652c0c` fix: registration 'Database error saving new user' — enum cast
- `8c64b93` fix: 'Could not start conversation' — DM RLS too strict

### Pending (needs APK v4)
- Infinite refresh fix (AuthContext + ProgressScreen deps)
- Pro player top-level message on Promotion tab (both student + coach sides)
- HomeScreen focus refetch
- MessagesScreen split DM/group layout
- Chat keyboard gap fix, optimistic updates, useFocusEffect reconnect (from earlier session)
- Coach direct message from StudentDetailScreen
- Promotion auto-start after every enrollment path

### Current DB State
- Fabian: `admin` role (NOT super_admin in profiles — super_admin is a separate enum value), enrolled in Amateur Division - Beginners (active)
- Josep (629f2752): `student`, division: `pro`, enrolled in Pro Division (active)
- Andrei (6af4e6d8): `student`, just registered successfully after registration fix
