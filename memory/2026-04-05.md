# Memory — 2026-04-05

## Nightly Backup (00:21 BST)

- Backup script ran automatically (separate process triggered before this cron session)
- Log: `/bpt-academy/backups/backup-2026-04-05.log` ✅
- Git: workspace + bpt-academy both auto-committed ✅
- Migrations snapshot saved: `migrations_20260405_002137.sql` ✅
- Credentials snapshot saved ✅
- Old backups cleaned (kept last 7 days) ✅

### Migration Sync Status (from backup log)

All migrations up to `20260327000001` synced with remote ✅

**Local-only (not yet applied to remote) ⚠️:**
- `20260328000001–20260328000008`
- `20260330000001` — coach_notes super_admin RLS fix
- `20260401000001` — fix email_notifications_enabled default (NULL → TRUE)
- `20260401000002` — welcome email trigger on registration
- `20260402000001` — fix video upload RLS + storage policy
- `20260403000001` — Parent/Junior account system (new `parent` role, parent_access table, dob column)
- `20260403000002` — fix parent_access RLS recursion
- `20260403000003` — fix profiles RLS recursion
- `20260403000004` — fix get_my_role() recursion
- `20260403000005` — fix get_my_role() bypass RLS
- `20260403000006` — DEFINITIVE RLS fix: rewrite get_my_role() using JWT claims (no table query)

---

## Summary of Recent Days (catch-up from 2026-04-01 to 2026-04-05)

### 2026-04-01 (APK crash fixes — already in memory/2026-04-01.md)
- Fixed xlsx lazy-load crash, EAS env vars, expo-notifications stub, SafeAreaProvider
- ⚠️ One more APK build still needed with SafeAreaProvider fix confirmed

### 2026-04-01 (migrations added same day)
- `20260401000001` — `email_notifications_enabled` default now TRUE for new users
- `20260401000002` — welcome email trigger on registration (Supabase edge function hook)

### 2026-04-02
- `20260402000001` — Fixed video upload RLS + storage policy: super_admin now included in upload policy

### 2026-04-03 (Parent/Junior system — major feature)
- Added `parent` to `user_role` enum
- Added `date_of_birth`, `parent_id`, `is_junior` columns to profiles
- New `parent_access` table (parent → child relationships)
- RLS policies for parent access (parents can view their children)
- Then: multi-step RLS recursion debugging (4 more migrations to fix infinite loop)
- Root cause: `get_my_role()` queried profiles → triggered RLS → queried profiles (loop)
- Final fix (`20260403000006`): rewrote `get_my_role()` to use JWT claims (no table query at all)
- `parent_access` admin policy also rewritten to use JWT claims directly

### 2026-04-04
- No memory file exists — likely a quiet day with no active development

### 2026-04-05 (today)
- No active development session — Sunday
- Nightly backup ran at 00:21 BST ✅

---

## Pending (carried forward)

- ⚠️ All migrations from 20260328* through 20260403* are **local-only** — not yet applied to remote Supabase
- ⚠️ One more APK build needed (SafeAreaProvider fix from 2026-04-01)
- ⚠️ expo-notifications / Firebase setup still pending (notifications blocked on Android)
- ⚠️ pg_cron schedules not yet set in Supabase for notification edge functions
- ⚠️ Josep Aymerich needs to register as a coach account
- ⚠️ Apple Developer account — Fabian creating it (needed for iOS EAS build + TestFlight)
- ⚠️ Test Parent/Junior login flow on device

---

## Session continued (afternoon, 2026-04-05)

### Key DB schema discoveries
- `programs` table has NO `status` column — uses `is_active` (boolean) instead
- `programs` table has `price_gbp` not `price`
- `profiles` table has NO `email` column — email lives in auth.users only
- These bugs caused attendance page to show no programs, and roster/enroll to show no students

### Fixes applied today
- Fixed `programs/page.tsx` — removed email from profiles queries (write tool)
- Fixed `attendance/page.tsx` — needs `is_active` filter + remove email
- Fixed `programs/page.tsx` — needs `is_active` and `price_gbp` schema alignment
- RLS policies: all admin/coach policies now use `auth.jwt() -> 'app_metadata' ->> 'role'`
- pg_cron schedules set on remote for all 3 edge functions
- RESEND_API_KEY secret set — emails now working
- send-notification edge function deployed
- Tournaments page: create/edit/delete all working
- Payments page: method, bank_reference, mark paid/fail
- Messaging page: Supabase Realtime live updates

### Dashboard deploys today
- All pushes to https://github.com/bptacademy/bpt-academy-dashboard.git auto-deploy to app.bptacademy.uk
- Last push: f1512c2 (fix email column bug in programs roster)

### Pending fixes still needed
- attendance/page.tsx: change `status='active'` → `is_active=true`, remove email column
- programs/page.tsx: align with actual schema (is_active, price_gbp)
- messaging: fix new conversation creation + add bulk announce feature
- Attendance reminder notification system (migration + edge function update)

---

## PENDING WORK (interrupted by memory flush at 17:09)

### 1. Fix attendance/page.tsx
- Change `.eq('status', 'active')` → `.eq('is_active', true)` in programs query
- Remove `email` from Student interface, profiles join, table header and table row
- Enrollments filter should also add `.eq('status', 'active')`

### 2. Fix programs/page.tsx  
- `programs` table: NO `status` column → use `is_active` (boolean)
- `programs` table: NO `price` column → use `price_gbp`
- Program interface: `status: string` → `is_active: boolean`, `price: number|null` → `price_gbp: number|null`
- Form default: `status: 'active'` → `is_active: true`
- STATUSES dropdown → Active/Inactive toggle
- All `program.status` → `program.is_active ? 'active' : 'inactive'`
- All `program.price` → `program.price_gbp`
- Save payload: use `is_active` and `price_gbp`

### 3. Fix messaging/page.tsx
- New conversation creation may fail silently — add console.error logging
- Add "📢 Announce to Division" button — simpler modal: pick division, write message, sends to all students in that division
- Conversation type: 'announcement', title: "Announcement: [Division] - [date]"

### 4. Attendance reminders
- Create migration: `20260405000003_attendance_reminder.sql`
- Function: `get_sessions_needing_attendance_reminder()` — finds sessions ended in last 2h with no attendance
- Update `session-reminders` edge function to call this and notify coaches + admins

### 5. Enrollments table
- `enrollments` table has a `status` column (active/cancelled/pending) — confirmed working
